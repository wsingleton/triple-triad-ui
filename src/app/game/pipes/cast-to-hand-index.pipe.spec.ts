import { CastToHandIndexPipe } from './cast-to-hand-index.pipe';

describe('CastToHandIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new CastToHandIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
